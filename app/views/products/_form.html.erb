<%= form_for @product, validate: true do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= #image_tag(@product.image_url) if @product.image? %><br>
    <%= f.label "Foto de Producto" %><br>
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label "Nombre de producto" %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "precio" %><br>
    <%= f.number_field :price %>
  </div>
  <div class="field">
    <%= f.label "Descripción" %><br>
    <%= f.text_area :detail %>
  </div>
  <div class="field">
    <%= f.label "Escoga la tienda en la que agregara el Producto" %><br>
    <%= f.collection_select :shop_id, @shops, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-primary', role: 'button', class: 'btn btn-success btn-sm' %>
  </div>
<% end %>
